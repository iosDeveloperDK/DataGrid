import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#99afc7'
    },
    secondary: {
      main: '#6559fe',
      clear: '#ff619d'
    }
  }
})

export default theme
