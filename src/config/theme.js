import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#99afc7'
    },
    secondary: {
      main: '#6559fe',
      clear: '#ff619d',
      border: '#dee8f4'
    },
    text: {
      main: '#4e6296'
    }
  }
})

export default theme
